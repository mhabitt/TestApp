import LoginForm from "./components/LoginForm";

const LoginPage = () => {
    return (
        <div className={"security-container"}>
            <LoginForm/>
        </div>
    );
};

export default LoginPage;
